[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    reproducibility_ci: reproducibility drills that are lightweight enough for normal CI.
    reproducibility_scheduled: higher-cost reproducibility drills intended for scheduled CI only.
    reproducibility_manual: manual-only reproducibility drills for expensive or environment-specific replay checks.
