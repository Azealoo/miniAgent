## README Refresh

Date: 2026-04-03

Goal:
- Update `README.md` so it reflects the current BioAPEX codebase rather than the earlier lightweight-agent prototype.

Scope:
- Re-scan the active backend and frontend architecture.
- Replace stale endpoint, tool, memory, and session-compression documentation.
- Document current repo structure, workflow/spec layout, access-control model, and verification commands.

Verification:
- Cross-check the rewritten README against `backend/app.py`, `backend/api/*`, `backend/tools/*`, `backend/graph/*`, `frontend/package.json`, and the current repo layout.
