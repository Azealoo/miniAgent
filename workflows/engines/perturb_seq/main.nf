#!/usr/bin/env nextflow

/*
 * Placeholder Nextflow entrypoint for the workflow-spec example.
 * This file exists so the authored workflow spec references a real repo path
 * without implying that the external-engine adapter is implemented in this phase.
 */

workflow {
    log.info "Placeholder perturb-seq Nextflow workflow entrypoint."
}
